{
  "name": "opexnow-stamp",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant e-Meterai stamping API + admin console (OpexNow Stamp)",
  "type": "module",
  "workspaces": [
    "worker",
    "web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace worker",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web && npm run check --workspace worker",
    "db:apply": "npm run db:apply --workspace worker",
    "db:seed": "npm run db:seed --workspace worker"
  }
}
